/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If deploying to GitHub Pages subdirectory, uncomment:
  // basePath: '/repository-name',
  // output: 'export', // For static export if needed
}

export default nextConfig

